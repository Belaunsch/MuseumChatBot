# Credentials Konfiguration für verschiedene Kanäle
# Diese Datei definiert die Verbindungseinstellungen für Chat-Kanäle

# Standard Rasa Shell/REST API
# Für lokale Tests und REST API
rasa:
  url: "http://localhost:5005/api"

# REST Channel für HTTP-basierte Kommunikation
rest:
  # Keine spezielle Konfiguration nötig

# Socketio Channel für Web-Interface
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true

# Facebook Messenger (Beispiel)
# facebook:
#   verify: "your-verify-token"
#   secret: "your-page-access-token"
#   page-access-token: "your-page-access-token"

# Slack Integration (Beispiel)
# slack:
#   slack_token: "xoxb-your-slack-bot-token"
#   slack_channel: "#your-channel"
#   slack_signing_secret: "your-signing-secret"

# Telegram Integration (Beispiel)
# telegram:
#   access_token: "your-telegram-bot-token"
#   verify: "your-verify-token"
#   webhook_url: "https://your-domain.com/webhooks/telegram/webhook"

# Google Assistant (Beispiel)
# google:
#   project_id: "your-google-project-id"

# Microsoft Bot Framework (Beispiel)
# botframework:
#   app_id: "your-app-id"
#   app_password: "your-app-password"

# Custom Webhook Channel (für eigene Integrationen)
# webhook:
#   webhook_token: "your-webhook-token"
